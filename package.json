{
  "name": "ultimate-harness",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "uh": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.tests.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "latest",
    "yaml": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
