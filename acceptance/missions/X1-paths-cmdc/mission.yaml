schema_version: uh.mission.v0
id: x1-paths-cmdc-acceptance
title: Canonical forward-slash paths
workflow_profile: bugfix-contained
objective: Create out/paths.txt containing exactly the single line paths, then stop.
constraints:
  - Do not run git.
  - Do not install packages.
  - Keep all files under out/.
verification:
  required_checks:
    - name: noop
      command: node -e "process.exit(0)"
runtime_config_overrides:
  permission_mode: yolo
sandbox:
  backend: git-worktree
  promotion_policy: human-approved
