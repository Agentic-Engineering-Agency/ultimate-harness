diff --git a/.harness/audit/events.ndjson b/.harness/audit/events.ndjson
index 297ee422..7be232de 100644
--- a/.harness/audit/events.ndjson
+++ b/.harness/audit/events.ndjson
@@ -1 +1,2 @@
 {"event":"project.init","timestamp":"2026-05-13T15:35:45.188Z","root":"/Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness","force":false}
+{"event":"mission.run","timestamp":"2026-05-25T07:35:21.213Z","runtime":"codex","mission_id":"implement-uh-slice-template","mission_name":"Dogfood: canonical implement-uh-slice mission template","workflow":"staged","run_id":"20260525T073520Z-59a335"}
diff --git a/.harness/missions/implement-uh-slice-template/latest.json b/.harness/missions/implement-uh-slice-template/latest.json
new file mode 100644
index 00000000..888ac96a
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/latest.json
@@ -0,0 +1,6 @@
+{
+  "schema_version": "uh.latest-run.v0",
+  "run_id": "20260525T073520Z-59a335",
+  "started_at": "2026-05-25T07:35:21.205Z",
+  "status": "running"
+}
\ No newline at end of file
diff --git a/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/events.ndjson b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/events.ndjson
new file mode 100644
index 00000000..e4a8f287
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/events.ndjson
@@ -0,0 +1 @@
+{"event":"runtime.started","timestamp":"2026-05-25T07:35:21.205Z","runtime":"codex","mission_id":"implement-uh-slice-template","command":"codex","args":["exec","--cd","/Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author","--sandbox","workspace-write","--json","--output-last-message","/Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-final.txt","--skip-git-repo-check","# Mission: Dogfood: canonical implement-uh-slice mission template\n\nCreate the canonical mission template that UH itself will use to implement any future UH slice (one Linear UH-NNN ticket = one mission). This is the first dogfood-mode mission: UH-on-UH. Success here proves the team mission shape (UH-72) + staged workflow profile (UH-73) can produce a usable artifact end-to-end without a human in the implementation loop. Failure here is itself the most diagnostic information available — it tells us which of the six gaps documented in the v0.8.0 post-mortem actually bite.\nDeliverables:\n  1. examples/missions/templates/implement-uh-slice.yaml — a parameterized\n     canonical mission packet that another mission author can copy and\n     fill in for any UH-NNN slice. MUST use `shape: team`,\n     `workflow_profile: staged`, complete `acceptance_criteria[]` block,\n     and `verification.required_checks[]` that enforce typecheck + tests\n     at minimum.\n  2. examples/missions/templates/README.md — one-page operator guide:\n     \"Copy this when implementing a UH-NNN Linear slice. Substitute the\n     <ALL_CAPS> placeholders. Then `uh mission run-team <id>`.\"\n\nNon-goals:\n  - Implementing any real UH-NNN slice via the template (that's the next\n    mission, not this one).\n  - Adding `uh mission promote --pr` or Linear-state-flip (those are\n    separate slices flagged in the post-mortem).\n  - Changing the mission schema, staged workflow profile, or team-run.ts\n    runtime. The template MUST be expressible under today's schema.\n\n## Workflow: Staged (Plan → PRD → Execute → Verify → Fix)\n\n### plan (architect)\nProduce a plan.md outlining the approach before any code lands.\n\n### prd (product-manager)\nCapture the contract / scope / acceptance in prd.md.\n\n### execute (developer)\nImplement the change. For team missions, workers fan out in parallel.\n\n### verify (reviewer)\nRun mission verification against the executed result.\n\n### fix (developer)\nAddress verification findings. Loops back into verify up to mission.verification.max_iterations times.\n\n## Related Issues\n- [linear] UH-126 (https://linear.app/agenticengineering-agency/team/UH/active)\n\n## Read First\n- src/schema/mission.ts\n- src/harness/team-run.ts\n- .harness/workflows/staged.yaml\n- examples/missions/documentation-spine.yaml\n- examples/missions/codex-e2e-smoke.yaml\n- tests/team-schema.test.ts\n- docs/ROADMAP.md\n- docs/prds/uh-roadmap-to-1.0-v1.0-prd.md\n\n## Expected Artifacts\n- examples/missions/templates/implement-uh-slice.yaml\n- examples/missions/templates/README.md\n\n## Verification Checks\n- bun run typecheck\n- bun -e 'import(\"./src/schema/mission.ts\").then(async m => { const yaml = await import(\"yaml\"); const fs = await import(\"node:fs/promises\"); const doc = yaml.parse(await fs.readFile(\"examples/missions/templates/implement-uh-slice.yaml\", \"utf8\")); m.validateMission(doc); console.log(\"OK\"); })'\n- test -f examples/missions/templates/README.md\n\nExecute this mission and produce the expected artifacts.\n\n## Runtime final message\n\nAt the very end of your response, emit your one-paragraph summary inside a fenced code block tagged `uh-runtime-final-message`:\n\n```uh-runtime-final-message\n<one-paragraph summary of what you did, what changed, and any caveats>\n```\n\nThis fenced block MUST be the last block in your output. The harness extracts its content verbatim into `runtime-final.txt`.\n"],"run_id":"20260525T073520Z-59a335"}
diff --git a/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/prompt.md b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/prompt.md
new file mode 100644
index 00000000..7a5bbb53
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/prompt.md
@@ -0,0 +1,72 @@
+# Mission: Dogfood: canonical implement-uh-slice mission template
+
+Create the canonical mission template that UH itself will use to implement any future UH slice (one Linear UH-NNN ticket = one mission). This is the first dogfood-mode mission: UH-on-UH. Success here proves the team mission shape (UH-72) + staged workflow profile (UH-73) can produce a usable artifact end-to-end without a human in the implementation loop. Failure here is itself the most diagnostic information available — it tells us which of the six gaps documented in the v0.8.0 post-mortem actually bite.
+Deliverables:
+  1. examples/missions/templates/implement-uh-slice.yaml — a parameterized
+     canonical mission packet that another mission author can copy and
+     fill in for any UH-NNN slice. MUST use `shape: team`,
+     `workflow_profile: staged`, complete `acceptance_criteria[]` block,
+     and `verification.required_checks[]` that enforce typecheck + tests
+     at minimum.
+  2. examples/missions/templates/README.md — one-page operator guide:
+     "Copy this when implementing a UH-NNN Linear slice. Substitute the
+     <ALL_CAPS> placeholders. Then `uh mission run-team <id>`."
+
+Non-goals:
+  - Implementing any real UH-NNN slice via the template (that's the next
+    mission, not this one).
+  - Adding `uh mission promote --pr` or Linear-state-flip (those are
+    separate slices flagged in the post-mortem).
+  - Changing the mission schema, staged workflow profile, or team-run.ts
+    runtime. The template MUST be expressible under today's schema.
+
+## Workflow: Staged (Plan → PRD → Execute → Verify → Fix)
+
+### plan (architect)
+Produce a plan.md outlining the approach before any code lands.
+
+### prd (product-manager)
+Capture the contract / scope / acceptance in prd.md.
+
+### execute (developer)
+Implement the change. For team missions, workers fan out in parallel.
+
+### verify (reviewer)
+Run mission verification against the executed result.
+
+### fix (developer)
+Address verification findings. Loops back into verify up to mission.verification.max_iterations times.
+
+## Related Issues
+- [linear] UH-126 (https://linear.app/agenticengineering-agency/team/UH/active)
+
+## Read First
+- src/schema/mission.ts
+- src/harness/team-run.ts
+- .harness/workflows/staged.yaml
+- examples/missions/documentation-spine.yaml
+- examples/missions/codex-e2e-smoke.yaml
+- tests/team-schema.test.ts
+- docs/ROADMAP.md
+- docs/prds/uh-roadmap-to-1.0-v1.0-prd.md
+
+## Expected Artifacts
+- examples/missions/templates/implement-uh-slice.yaml
+- examples/missions/templates/README.md
+
+## Verification Checks
+- bun run typecheck
+- bun -e 'import("./src/schema/mission.ts").then(async m => { const yaml = await import("yaml"); const fs = await import("node:fs/promises"); const doc = yaml.parse(await fs.readFile("examples/missions/templates/implement-uh-slice.yaml", "utf8")); m.validateMission(doc); console.log("OK"); })'
+- test -f examples/missions/templates/README.md
+
+Execute this mission and produce the expected artifacts.
+
+## Runtime final message
+
+At the very end of your response, emit your one-paragraph summary inside a fenced code block tagged `uh-runtime-final-message`:
+
+```uh-runtime-final-message
+<one-paragraph summary of what you did, what changed, and any caveats>
+```
+
+This fenced block MUST be the last block in your output. The harness extracts its content verbatim into `runtime-final.txt`.
diff --git a/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-final.txt b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-final.txt
new file mode 100644
index 00000000..94f1b548
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-final.txt
@@ -0,0 +1,19 @@
+`★ Insight ─────────────────────────────────────`
+The important schema constraint here is that the template has to be both copyable and immediately valid. That’s why adapter ids are concrete `codex` defaults while slice-specific values remain `<ALL_CAPS>` placeholders in paths, names, commands, and issue references.
+`─────────────────────────────────────────────────`
+
+Implemented the canonical UH slice mission template and operator guide:
+
+- Added [implement-uh-slice.yaml](/Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author/examples/missions/templates/implement-uh-slice.yaml)
+- Added [README.md](/Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author/examples/missions/templates/README.md)
+
+Validation evidence:
+
+- `bun run typecheck` passed.
+- Mission schema validation command passed with `OK`.
+- `test -f examples/missions/templates/README.md` passed with `OK`.
+- `bun run test` could not pass in this restricted worker environment. First run failed because Vite tried to write under the parent checkout’s `node_modules/.vite-temp`; retrying with `bunx vitest run --configLoader runner` avoided that startup issue but failed on sandbox/runtime constraints: localhost listen `EPERM`, missing worker-local `node_modules/.bin/tsx`, and git commit failures due 1Password signing socket access.
+
+```uh-runtime-final-message
+Created the canonical dogfood mission template at examples/missions/templates/implement-uh-slice.yaml and the one-page operator guide at examples/missions/templates/README.md. The template uses shape: team, workflow_profile: staged, a complete acceptance_criteria block, concrete valid codex team adapter defaults, and verification.required_checks that enforce typecheck and tests. Verified with bun run typecheck, direct validateMission YAML parsing, and README existence; the broad test suite was attempted but is blocked by this worker sandbox’s process/network/git-signing restrictions rather than the new template.
+```
\ No newline at end of file
diff --git a/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-session.yaml b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-session.yaml
new file mode 100644
index 00000000..b6064115
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-session.yaml
@@ -0,0 +1,140 @@
+schema_version: uh.runtime-session.v0
+mission_id: implement-uh-slice-template
+runtime: codex
+status: running
+command: codex
+args:
+  - exec
+  - --cd
+  - /Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author
+  - --sandbox
+  - workspace-write
+  - --json
+  - --output-last-message
+  - /Users/eduardojaviergarcialopez/AgenticEngineering/ultimate-harness/.harness/missions/implement-uh-slice-template/team/workers/template-author/.harness/missions/implement-uh-slice-template/runs/20260525T073520Z-59a335/runtime-final.txt
+  - --skip-git-repo-check
+  - >
+    # Mission: Dogfood: canonical implement-uh-slice mission template
+
+
+    Create the canonical mission template that UH itself will use to implement
+    any future UH slice (one Linear UH-NNN ticket = one mission). This is the
+    first dogfood-mode mission: UH-on-UH. Success here proves the team mission
+    shape (UH-72) + staged workflow profile (UH-73) can produce a usable
+    artifact end-to-end without a human in the implementation loop. Failure here
+    is itself the most diagnostic information available — it tells us which of
+    the six gaps documented in the v0.8.0 post-mortem actually bite.
+
+    Deliverables:
+      1. examples/missions/templates/implement-uh-slice.yaml — a parameterized
+         canonical mission packet that another mission author can copy and
+         fill in for any UH-NNN slice. MUST use `shape: team`,
+         `workflow_profile: staged`, complete `acceptance_criteria[]` block,
+         and `verification.required_checks[]` that enforce typecheck + tests
+         at minimum.
+      2. examples/missions/templates/README.md — one-page operator guide:
+         "Copy this when implementing a UH-NNN Linear slice. Substitute the
+         <ALL_CAPS> placeholders. Then `uh mission run-team <id>`."
+
+    Non-goals:
+      - Implementing any real UH-NNN slice via the template (that's the next
+        mission, not this one).
+      - Adding `uh mission promote --pr` or Linear-state-flip (those are
+        separate slices flagged in the post-mortem).
+      - Changing the mission schema, staged workflow profile, or team-run.ts
+        runtime. The template MUST be expressible under today's schema.
+
+    ## Workflow: Staged (Plan → PRD → Execute → Verify → Fix)
+
+
+    ### plan (architect)
+
+    Produce a plan.md outlining the approach before any code lands.
+
+
+    ### prd (product-manager)
+
+    Capture the contract / scope / acceptance in prd.md.
+
+
+    ### execute (developer)
+
+    Implement the change. For team missions, workers fan out in parallel.
+
+
+    ### verify (reviewer)
+
+    Run mission verification against the executed result.
+
+
+    ### fix (developer)
+
+    Address verification findings. Loops back into verify up to
+    mission.verification.max_iterations times.
+
+
+    ## Related Issues
+
+    - [linear] UH-126
+    (https://linear.app/agenticengineering-agency/team/UH/active)
+
+
+    ## Read First
+
+    - src/schema/mission.ts
+
+    - src/harness/team-run.ts
+
+    - .harness/workflows/staged.yaml
+
+    - examples/missions/documentation-spine.yaml
+
+    - examples/missions/codex-e2e-smoke.yaml
+
+    - tests/team-schema.test.ts
+
+    - docs/ROADMAP.md
+
+    - docs/prds/uh-roadmap-to-1.0-v1.0-prd.md
+
+
+    ## Expected Artifacts
+
+    - examples/missions/templates/implement-uh-slice.yaml
+
+    - examples/missions/templates/README.md
+
+
+    ## Verification Checks
+
+    - bun run typecheck
+
+    - bun -e 'import("./src/schema/mission.ts").then(async m => { const yaml =
+    await import("yaml"); const fs = await import("node:fs/promises"); const doc
+    = yaml.parse(await
+    fs.readFile("examples/missions/templates/implement-uh-slice.yaml", "utf8"));
+    m.validateMission(doc); console.log("OK"); })'
+
+    - test -f examples/missions/templates/README.md
+
+
+    Execute this mission and produce the expected artifacts.
+
+
+    ## Runtime final message
+
+
+    At the very end of your response, emit your one-paragraph summary inside a
+    fenced code block tagged `uh-runtime-final-message`:
+
+
+    ```uh-runtime-final-message
+
+    <one-paragraph summary of what you did, what changed, and any caveats>
+
+    ```
+
+
+    This fenced block MUST be the last block in your output. The harness
+    extracts its content verbatim into `runtime-final.txt`.
+started_at: 2026-05-25T07:35:21.205Z
diff --git a/.harness/missions/implement-uh-slice-template/runs/index.json b/.harness/missions/implement-uh-slice-template/runs/index.json
new file mode 100644
index 00000000..d2b16d31
--- /dev/null
+++ b/.harness/missions/implement-uh-slice-template/runs/index.json
@@ -0,0 +1,11 @@
+{
+  "schema_version": "uh.runs-index.v0",
+  "runs": [
+    {
+      "run_id": "20260525T073520Z-59a335",
+      "started_at": "2026-05-25T07:35:21.205Z",
+      "status": "running",
+      "runtime": "codex"
+    }
+  ]
+}
\ No newline at end of file
diff --git a/examples/missions/templates/README.md b/examples/missions/templates/README.md
new file mode 100644
index 00000000..ef779c9d
--- /dev/null
+++ b/examples/missions/templates/README.md
@@ -0,0 +1,26 @@
+# Implement UH Slice Mission Template
+
+Copy `implement-uh-slice.yaml` when implementing one Linear UH-NNN slice as a team mission.
+
+## How To Use
+
+1. Copy `examples/missions/templates/implement-uh-slice.yaml` to a mission-specific file or mission packet location.
+2. Substitute every `<ALL_CAPS>` placeholder:
+   - `<UH-NNN>`: the Linear issue id, for example `UH-126`.
+   - `<SLICE_NAME>`: a short human-readable slice name.
+   - `<PRIMARY_CODE_OR_DOC_PATH>` and `<SECONDARY_CODE_OR_DOC_PATH>`: the most important files the mission agents should read first.
+   - `<EXPECTED_ARTIFACT_PATH>`: the primary file the slice must create or modify.
+   - `<EXPECTED_TEST_PATH>`: the targeted regression test file for the slice.
+3. Adjust `team.workers` only if the slice needs different valid adapter ids or more parallel lanes.
+4. Keep `shape: team`, `workflow_profile: staged`, `acceptance_criteria[]`, and the typecheck/test entries in `verification.required_checks[]`.
+5. Run the mission:
+
+```sh
+uh mission run-team <id>
+```
+
+## Operator Notes
+
+The template is intentionally valid under today's schema before placeholder substitution. Adapter ids use concrete defaults because the schema validates them against the registered team adapters. Placeholder values are reserved for mission-specific text, paths, commands, and Linear references.
+
+Use this for implementation missions only. Do not use it to promote PRs, flip Linear state, or implement multiple UH slices in one packet.
diff --git a/examples/missions/templates/implement-uh-slice.yaml b/examples/missions/templates/implement-uh-slice.yaml
new file mode 100644
index 00000000..4b6656fd
--- /dev/null
+++ b/examples/missions/templates/implement-uh-slice.yaml
@@ -0,0 +1,105 @@
+schema_version: uh.mission.v0
+id: <UH-NNN>-implement-slice
+title: <UH-NNN> Implement <SLICE_NAME>
+workflow_profile: staged
+priority: high
+objective: >-
+  Implement the <UH-NNN> Linear slice end-to-end under the staged workflow.
+  Produce a plan and PRD first, execute the scoped change in a team mission,
+  verify with required checks, and address verification findings within the
+  bounded fix loop.
+issue_refs:
+  - provider: linear
+    id: <UH-NNN>
+    url: https://linear.app/agenticengineering-agency/issue/<UH-NNN>
+context:
+  repo_root: .
+  read_first:
+    - src/schema/mission.ts
+    - src/harness/team-run.ts
+    - .harness/workflows/staged.yaml
+    - <PRIMARY_CODE_OR_DOC_PATH>
+    - <SECONDARY_CODE_OR_DOC_PATH>
+  source_links:
+    - https://linear.app/agenticengineering-agency/issue/<UH-NNN>
+constraints:
+  - Keep the change expressible under today's mission schema and runtime.
+  - Keep diffs small, reviewable, and scoped to <UH-NNN>.
+  - Do not implement unrelated slices or change Linear promotion behavior.
+  - Preserve existing public contracts unless the PRD explicitly narrows the change.
+skills:
+  suggested:
+    - debug-like-expert
+    - lint
+shape: team
+team:
+  leader:
+    adapter: codex
+    role: integrator
+  workers:
+    - adapter: codex
+      role: implementation
+      count: 1
+    - adapter: codex
+      role: verification
+      count: 1
+integration_report_path: .harness/missions/<UH-NNN>-implement-slice/team/integration-report.md
+expected_outputs:
+  files:
+    - <EXPECTED_ARTIFACT_PATH>
+    - <EXPECTED_TEST_PATH>
+completion_criteria:
+  - "plan.md exists and explains the implementation approach before code changes."
+  - "prd.md captures scope, non-goals, acceptance criteria, and verification contract."
+  - "<EXPECTED_ARTIFACT_PATH> implements the <UH-NNN> slice."
+  - "<EXPECTED_TEST_PATH> covers the changed behavior."
+  - "All required verification checks pass."
+acceptance_criteria:
+  - id: plan-artifact
+    description: "The staged plan phase produced plan.md before implementation work."
+    check_command: "test -f .harness/missions/<UH-NNN>-implement-slice/plan.md"
+    severity: block
+  - id: prd-artifact
+    description: "The staged PRD phase produced prd.md with scope, non-goals, and acceptance criteria."
+    check_command: "test -f .harness/missions/<UH-NNN>-implement-slice/prd.md"
+    severity: block
+  - id: expected-artifact
+    description: "The slice's primary expected artifact exists and is ready for review."
+    check_command: "test -f <EXPECTED_ARTIFACT_PATH>"
+    severity: block
+  - id: regression-coverage
+    description: "The slice includes targeted regression coverage for the changed behavior."
+    check_command: "test -f <EXPECTED_TEST_PATH>"
+    severity: block
+  - id: required-checks-pass
+    description: "Typecheck and test commands pass before the mission is complete."
+    check_command: "bun run typecheck && bun run test"
+    severity: block
+tdd:
+  enforce_tests_first: true
+  test_paths:
+    - tests/**
+    - "**/*.test.ts"
+    - "**/*.spec.ts"
+  source_paths:
+    - src/**
+    - examples/**
+runtime_requirements:
+  needs_network: false
+  needs_shell: true
+  needs_fs_write: true
+  max_cost_class: premium
+verification:
+  max_iterations: 2
+  required_checks:
+    - name: typecheck
+      command: bun run typecheck
+    - name: tests
+      command: bun run test
+    - name: expected artifact exists
+      command: test -f <EXPECTED_ARTIFACT_PATH>
+    - name: expected test exists
+      command: test -f <EXPECTED_TEST_PATH>
+  review_gates:
+    - "Reviewer confirms the PRD contract and implementation diff match <UH-NNN>."
+    - "Reviewer confirms no non-goal slice or promotion behavior was implemented."
